# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: db-pvc
# spec:
#   accessModes:
#     - ReadWriteOnce
#   storageClassName: "standard"
#   resources:
#     requests:
#       storage: 5Gi
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: regional-standard
  resources:
    requests:
      storage: 200Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elgg-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: regional-standard
  resources:
    requests:
      storage: 200Gi
---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elgg-config-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: regional-standard
  resources:
    requests:
      storage: 200Gi
